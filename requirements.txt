# CryptoTracker Pro — Python (Streamlit) edition
# Core stack (always required). Installs cleanly on Python 3.9+.
streamlit>=1.50          # uses width="stretch" + hide_index in st.dataframe/button
requests>=2.31
pandas>=2.0
numpy>=1.24
plotly>=5.20

# Forecasting ensemble (Future page):
statsmodels>=0.14        # Holt / Theta / ARIMA + AIC selection
scikit-learn>=1.3        # HistGradientBoosting model in the ensemble

# Better data + sentiment (the app degrades gracefully if these are missing):
ccxt>=4.0                # Binance daily OHLCV (key-less) for forecasting history;
                         # falls back to CoinGecko when unavailable
vaderSentiment>=3.3      # news headline sentiment (falls back to "Neutral")
feedparser>=6.0          # keyless RSS news fallback (works with no API key)

# Optional (Live Paper-Trading Lab): activates only if installed, degrades silently:
# river>=0.21            # ADWIN concept-drift detection (else a rolling-error detector is used)
